{
  "name": "dollhousedev",
  "version": "0.1.0",
  "private": true,
  "description": "Spatial multi-agent AI dev environment with a 3D pink Victorian dollhouse UI",
  "type": "module",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,magenta \"pnpm --filter @dollhouse/backend dev\" \"pnpm --filter @dollhouse/frontend dev\"",
    "build": "pnpm --filter @dollhouse/shared build && pnpm --filter @dollhouse/editor build && pnpm --filter @dollhouse/backend build && pnpm --filter @dollhouse/frontend build",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r exec rm -rf dist node_modules .turbo"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@11.1.3",
  "engines": {
    "node": ">=20"
  }
}
